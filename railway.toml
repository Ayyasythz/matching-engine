[build]
builder = "nixpacks"
buildCommand = "go build -o server ./cmd/server"

[deploy]
startCommand = "./server -frontend ./frontend"
healthcheckPath = "/api/book"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
